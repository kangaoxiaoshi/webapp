var config = {
  // 项目根路径
  baseUrl: 'accident',
  routesList: [

  ],
  support: {
    storage: true
  },
  waitTime: 5 * 60 * 1000,
  version: '2.0.0'
};

var requirejs = {
  //urlArgs: Date.now(),
  //urlArgs: 3,
  paths: {
  }
};

